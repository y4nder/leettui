---
phase: 02.1-manage-local-test-case-expected-outputs-golden-snapshot-work
verified: 2026-07-11T00:00:00Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 2.1: Local Test-Case Output Management Verification Report

**Phase Goal:** The user can populate and edit a case's expected output (`case-NN.out`) without hand-writing files, so local `leettui test` grades real pass/fail instead of the verdict-less `ran`. Closes the gap that seeded `tests/case-NN.txt` ship inputs only.
**Verified:** 2026-07-11
**Status:** passed
**Re-verification:** No — initial verification

## Verification Method

This report does **not** rely on SUMMARY.md's self-reported "manual_procedural" claims. Both plans' human-check tasks were self-attested as automated by the executor in a sandboxed session — so, per the adversarial-verification mandate, I independently rebuilt an equivalent fixture from scratch and drove the actual compiled CLI entry point (`bun src/index.tsx test [--save|--add-case]`) myself, in a throwaway `$HOME` (temp dir, `XDG_DATA_HOME`/`XDG_CONFIG_HOME` cleared), against a fabricated `questions` row (`1. Two Sum`) and a real `createSolutionWithHarness`-generated python3 solution + harness + 2 seeded cases. All commands below were actually executed during this verification pass (not copied from the SUMMARY), and I confirmed the real solutions repo (`~/Documents/codes/leetcode-solutions-yander`) was untouched (`git status`/`git diff` clean for `0001_two-sum/`) after an initial config-resolution mistake was caught and verified harmless (create-if-absent skipped every write since the real files already existed).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `leettui test --save` writes `case-NN.out` for every cleanly-run case from the current run's stdout | ✓ VERIFIED | Live run: plain `test` on 2 seeded cases showed "Ran 2 cases (no expected outputs)"; `test --save` printed `+ case-01 created`, `+ case-02 created`, exit 0 |
| 2 | A case that errored/timed out is skipped by `--save` and the skip is reported | ✓ VERIFIED | Live run: swapped in a `raise RuntimeError("boom")` solution, ran `test --save` → `! case-01 skipped` / `! case-02 skipped` / `! case-03 skipped`, "0 written · 3 skipped", exit 2; prior `.out` files' md5sums confirmed unchanged after |
| 3 | Re-running `--save` overwrites an existing `.out` and reports created/changed/unchanged/skipped per case | ✓ VERIFIED | Live run: 2nd `--save` on unchanged solution reported `= case-01 unchanged`, `= case-02 unchanged`; after `--add-case` added case-03, a further `--save` reported `+ case-03 created` alongside the two `unchanged` |
| 4 | After `--save`, the next `leettui test` discovers the written `.out` and grades that case pass instead of `ran` | ✓ VERIFIED | Live run: after `--save`, plain `test` reported "✓ Local: 2/2 passed" (later 3/3 once case-03 joined) |
| 5 | `--save` never touches a case it did not run (unrelated sibling `.out` stays byte-for-byte intact) | ✓ VERIFIED | Live run: manually created `case-99.out` = `ORPHAN_CONTENT` before any `--save`; content verbatim-unchanged across 3 subsequent `--save` invocations (incl. the all-skipped crash run) |
| 6 | `leettui test --add-case` reads a new case input from stdin (or a file argument) and writes it as the next sequential `case-NN.txt` | ✓ VERIFIED | Live run: `printf '[3,3]\n6\n' \| test --add-case` → `created: …/tests/case-03.txt`, exit 0; file content verbatim `[3,3]\n6\n` |
| 7 | Next case number is `max(existing) + 1`, 2-digit zero-padded, so `discoverCases`/`pairCases` order stays correct | ✓ VERIFIED | Live run + code: with `case-01`/`case-02` present, `--add-case` produced `case-03` (confirmed on disk); `nextCaseName`'s gap-safe max+1 logic unit-tested in `add-case.test.ts` (`case-01`,`case-03` → `case-04`) |
| 8 | The newly added input is picked up by `discoverCases`/`pairCases` on the next run with no runner changes | ✓ VERIFIED | Live run: immediately after `--add-case`, plain `test` showed the new `case-03` as a `·` (ran, no verdict) row alongside the passing `case-01`/`case-02`, with zero runner code changes needed |
| 9 | An empty stdin/file input writes nothing and exits 2, never creating a blank case | ✓ VERIFIED | Live run: `test --add-case < /dev/null` printed the branded "No case input" error, exit 2; `tests/` dir listing before/after identical (no new file) |
| 10 | `--add-case` only writes a new `case-NN.txt` input (never a `.out`); its expected output is blessed later by `--save` | ✓ VERIFIED | Live run: after `--add-case`, only `case-03.txt` existed (no `case-03.out`) until the subsequent `--save` created it |

**Score:** 10/10 truths verified (0 present-but-behavior-unverified)

### ROADMAP Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Snapshot current run's stdout into `case-NN.out` (golden/accept-output) so next `test` grades pass/fail | ✓ MET | Truths 1, 3, 4 above |
| 2 | Add a brand-new local case without hand-creating files, picked up by `discoverCases`/`pairCases` on the next run | ✓ MET | Truths 6-9 above |
| 3 | Case-writing path reuses the existing `tests/` layout + `compareOutput` grading (JSON-normalized), safe/idempotent, never clobbers an unrelated case | ✓ MET | `save-output.ts` imports and calls `compareOutput` from `../testRunner` for changed/unchanged; truth 5 (live orphan-file test) confirms the never-clobber invariant; `add-case.ts`/`save-output.ts` both contain no `try`/`catch` (never-throws, matching `seedTests`) |
| 4 | (If a TUI surface is chosen) a ProblemView action lets the user add/edit a case and accept output in-TUI | N/A — out of scope by design | discuss-phase resolved D-01: CLI-only surface, explicitly deferred to a later TUI re-skin (`02.1-CONTEXT.md` §Decisions/§Deferred Ideas). Per this verification's instructions, the absent TUI surface is not reported as a gap. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/solutions/save-output.ts` | `saveGoldenOutputs` writer, `SaveOutcome`/`SaveResult` types | ✓ VERIFIED | Exists, exports match, no `try`/`catch`, reuses `compareOutput`; exercised live |
| `src/core/solutions/save-output.test.ts` | Co-located tests | ✓ VERIFIED | 8 tests covering created/changed/unchanged/skipped, D-02/D-04 invariants, golden→grade loop — all pass |
| `src/core/solutions/add-case.ts` | `nextCaseName`/`addCase` writer | ✓ VERIFIED | Exists, exports match, no `try`/`catch`, calls `discoverCases`; exercised live |
| `src/core/solutions/add-case.test.ts` | Co-located tests | ✓ VERIFIED | 7 tests covering max+1/gap-safe numbering, D-05 .txt-only, round-trip via `discoverCases` — all pass |
| `src/core/solutions/index.ts` barrel | Re-exports `saveGoldenOutputs`/`SaveOutcome`/`SaveResult`/`addCase`/`nextCaseName` | ✓ VERIFIED | Confirmed present in barrel |
| `src/cli/present.ts` | `saveOutcomeGlyph`/`presentSaveSummary`/`saveReminder`/`exitCodeForSave` | ✓ VERIFIED | All present, exercised live (glyphs `+`/`~`/`=`/`!` all observed in live output) |
| `src/cli/index.ts` | `hasFlag`/`flagValue`, `--save`/`--add-case` branches, argv-threaded `runCli` | ✓ VERIFIED | Confirmed present and exercised live for both flags |
| `src/index.tsx` | Passes `process.argv` into `runCli`, drops unused `verbArg` import | ✓ VERIFIED | `runCli(verb, process.cwd(), process.argv)` confirmed at line 52; `verbArg` no longer destructured from the `cli` import |
| `.planning/REQUIREMENTS.md` | TCASE-01/02/03 formalized + traceability | ✓ VERIFIED | All three present, marked `[x]` Complete, traceability rows present, coverage total 21/21 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `src/cli/index.ts` `test` case | `saveGoldenOutputs(getTestsDir(...), report.cases)` | Consumes `runLocalTests`' `LocalCaseResult[]` directly, no re-run (D-12) | ✓ WIRED | Confirmed by reading `report.kind === "ran"` branch and by live run (single `runLocalTests` invocation feeds both the plain view and `--save`) |
| `saveGoldenOutputs` | `testRunner.compareOutput` | JSON-normalized changed/unchanged decision | ✓ WIRED | Import confirmed in `save-output.ts`; live run showed correct `unchanged` classification across re-saves of identical output |
| `src/index.tsx` | `runCli` | `process.argv` threaded through | ✓ WIRED | Confirmed at call site; live `--save`/`--add-case` flags were correctly recognized end-to-end |
| `nextCaseName` | `testRunner.discoverCases` | Computes next index from existing cases | ✓ WIRED | Import confirmed; live run produced `case-03` correctly after 2 existing cases |
| `src/cli/index.ts` `--add-case` branch | `addCase(getTestsDir(...), input)` | Reads stdin/file via `Bun.stdin.text()`/`Bun.file(path).text()` | ✓ WIRED | Confirmed live for both the bare-stdin form and (per plan 02 unit tests) the file-arg form |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| TCASE-01 | 02.1-01 | `--save` snapshots cleanly-run stdout into `case-NN.out`, jest-`-u` overwrite, skip error/timeout, report + reminder, never clobber | ✓ SATISFIED | Truths 1-5; live e2e |
| TCASE-02 | 02.1-02 | `--add-case` reads stdin/file, writes next sequential `case-NN.txt`, picked up by `discoverCases`/`pairCases` | ✓ SATISFIED | Truths 6-9; live e2e |
| TCASE-03 | 02.1-01 / 02.1-02 | Case-writing path reuses `tests/` layout + `compareOutput`, safe/idempotent/never-throws, never clobbers | ✓ SATISFIED | Truth 5, 10; code review (no try/catch, `compareOutput` reuse) |

No orphaned requirements — REQUIREMENTS.md maps exactly TCASE-01/02/03 to Phase 2.1, and both plans jointly declare all three in frontmatter.

### Anti-Patterns Found

None. Scanned all phase-modified files (`save-output.ts`, `add-case.ts`, `core/solutions/index.ts`, `cli/present.ts`, `cli/index.ts`, `src/index.tsx`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`, placeholder prose, empty-return stubs, and hardcoded-empty data — zero hits.

### Behavioral Spot-Checks / Live E2E

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plain `test` shows verdict-less `ran` | `bun src/index.tsx test` (fresh cases) | "Ran 2 cases (no expected outputs)" | ✓ PASS |
| `--save` blesses outputs | `bun src/index.tsx test --save` | `+ case-01 created`, `+ case-02 created`, exit 0 | ✓ PASS |
| Subsequent `test` grades pass | `bun src/index.tsx test` | "✓ Local: 2/2 passed" | ✓ PASS |
| Re-save unchanged | `bun src/index.tsx test --save` (same solution) | `= case-01 unchanged`, `= case-02 unchanged` | ✓ PASS |
| `--add-case` via stdin | `printf '[3,3]\n6\n' \| bun src/index.tsx test --add-case` | `created: …/case-03.txt`, exit 0 | ✓ PASS |
| Empty `--add-case` input | `bun src/index.tsx test --add-case < /dev/null` | branded error, exit 2, no file created | ✓ PASS |
| New case discovered pre-bless | `bun src/index.tsx test` | case-03 shown as `·` (ran, no verdict) | ✓ PASS |
| `--save` includes new case | `bun src/index.tsx test --save` | `+ case-03 created` alongside 2 `unchanged` | ✓ PASS |
| All 3 pass after full round-trip | `bun src/index.tsx test` | "✓ Local: 3/3 passed" | ✓ PASS |
| Crash is skipped, never blessed | `bun src/index.tsx test --save` (RuntimeError solution) | all 3 `skipped`, exit 2; prior `.out` md5sums unchanged | ✓ PASS |
| Unrelated case untouched | manual `case-99.out` = `ORPHAN_CONTENT` | byte-for-byte identical across every subsequent `--save` | ✓ PASS |

### Full Gate

`bun run check` (Biome zero-tolerance lint + `tsc --noEmit` + full `bun test`): **400 tests pass, 0 failures**, lint/typecheck clean. Task commits `b9d2ca2`, `f14f3a5`, `ccb760e`, `a64b114`, `088b68a` all confirmed present in `git log --oneline --all`.

### Human Verification Required

None. Every truth was independently exercised end-to-end against a live, isolated fixture during this verification pass (not merely inferred from presence/wiring), so no item is routed to human verification.

### Gaps Summary

No gaps. All 10 must-have truths and all 3 in-scope ROADMAP success criteria are verified against the actual running code, not just SUMMARY.md claims. Success criterion 4 (TUI surface) is explicitly out of scope per the phase's own documented D-01 decision and is correctly not implemented — this is by design, not a miss.

---

_Verified: 2026-07-11_
_Verifier: Claude (gsd-verifier)_
