---
phase: 02.2-auto-capture-failing-cases-from-run-submit-into-offline-regr
verified: 2026-07-11T00:00:00Z
status: passed
score: 4/4 must-haves verified (roadmap success criteria); 15/15 plan-level truths verified
behavior_unverified: 0
overrides_applied: 0
re_verification: null
human_verification:

  - test: "Run a real submission that returns Wrong Answer (or trigger a run that completes accepted/wrong-answer) through the TUI (R/s in ProblemView) and confirm a dim capture/blessing note appears below the error/metrics block in the inline Result panel, the ResultFullscreen takeover, and the ResultPopup."
    expected: "A dim confirmation line such as \"Captured case-05 (input + expected)\" or \"Blessed 2 expected output(s)\" appears after any error block and before the cases list, with no change to the verdict title color."
    why_human: "Visual placement/styling/legibility of a dim terminal line is a rendering concern that static code inspection and unit tests cannot fully confirm — this is exactly what SUMMARY 02.2-02 itself flagged (human_judgment: true, no snapshot/visual test exists for ResultBody.tsx)."

  - test: "Run `leettui run` and `leettui submit` from a terminal against a real failing solution and confirm the dim capture note prints below the error section in the CLI output, and that the exit code is unaffected (matches pre-existing wrong/error semantics)."
    expected: "presentResultView prints one dim line per capture note after the error block; `echo $?` after the command still reflects only the verdict (0/1/2 family), never altered by capture notes."
    why_human: "Same rendering-only concern as above, on the second (CLI) render surface; also confirms the never-throws contract holds against a live LeetCode round-trip, which unit tests (temp-dir fs only) cannot exercise end-to-end."
---

# Phase 02.2: Auto-Capture Failing Cases from Run/Submit into Offline Regression Cases Verification Report

**Phase Goal:** When a `run`/`submit` comes back wrong-answer, leettui auto-captures the failing input and expected output from LeetCode's `CheckResponse` into a new local `tests/case-NN.txt` + `case-NN.out`, turning any online failure into a permanent, reproducible OFFLINE regression case runnable via `leettui test`.

**Verified:** 2026-07-11
**Status:** human_needed
**Re-verification:** No — initial verification

## MVP Mode Note

ROADMAP.md marks this phase `Mode: mvp`, but the ROADMAP `**Goal:**` line itself is outcome-shaped, not in strict "As a X, I want Y, so that Z" form (`user-story.validate` on the raw ROADMAP goal returns `valid: false`). Plan 02.2-01 already flagged this explicitly and derived a compliant story from CONTEXT.md, which validates as `valid: true`:

> **As a** LeetCode solver working from the terminal, **I want to** have leettui automatically save a failing run/submit's input and expected output as an offline test case, **so that** I can reproduce and fix the bug locally with `leettui test` without repeatedly hitting LeetCode.

This verification uses that validated derived story for the User Flow Coverage table below, and the ROADMAP's four numbered Success Criteria as the authoritative truths (per the phase's own directive: "Success criteria (must be TRUE)"), consistent with how the plan itself scoped verification. No scope change is implied; `/gsd mvp-phase 2.2` can reformat the ROADMAP line itself if desired, but that is cosmetic and does not block this verification.

### User Flow Coverage

| # | Step | Expected | Evidence | Status |
|---|------|----------|----------|--------|
| 1 | User's `run`/`submit` comes back WA/RE/TLE/MLE | `CheckResponse` is already parsed into a `ParsedResponse` by the pre-existing `submission.ts` poll path | `src/core/submission.ts:145,173` (`pollResult`) — pre-existing, unchanged this phase | VERIFIED |
| 2 | leettui auto-captures the failing input + expected output | `captureFromSubmitResult`/`captureFromRunResult` invoke `captureFailingCase`/`blessRunOutputs`, writing `tests/case-NN.txt` (+`.out`) | `src/core/submission.ts:93-124`, `src/core/solutions/capture.ts:61-131`; unit tests pass (`capture.test.ts`, `submission.test.ts`) | VERIFIED |
| 3 | Capture is non-destructive and de-duplicated | D-04 (dup skip), D-05 (fill missing `.out`), D-06 (disagreeing `.out` never overwritten) all implemented and tested | `capture.ts:78-102`; `capture.test.ts` lines 43-108 (D-04/D-05/D-06 tests, all pass) | VERIFIED |
| 4 | User re-runs `leettui test` locally and it grades pass once fixed | Round-trip test: `captureFailingCase` → `discoverCases` finds a non-null `expectedPath` → `compareOutput` grades pass | `capture.test.ts:121-130` ("round-trip (ROADMAP criterion 3)"), passes | VERIFIED |
| 5 | User sees confirmation that capture happened, without disrupting the run/submit result | `notes?: string[]` threaded through `ResultView`, rendered dim in `ResultBody.tsx` + `present.ts`; `exitCodeForResultView`/verdict `kind` untouched | `src/views/browse/resultView/types.ts:36`, `src/ui/components/ResultBody.tsx:150-159`, `src/cli/present.ts:195-198`; code-level confirmed, **visual rendering not exercised by an automated test — routed to human verification** | PRESENT, visual confirmation needed |

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A wrong-answer verdict from `run`/`submit` writes the failing LeetCode input as a new `tests/case-NN.txt` and its `expected_output` as the sibling `case-NN.out`, using 2.1's case-writing seam | ✓ VERIFIED | `captureFailingCase` (`capture.ts:61-103`) reuses `addCase` (2.1) for the `.txt` write and writes `.out` from `expectedOutput`; `captureFromSubmitResult` (`submission.ts:93-109`) wires `submit_wrong_answer`'s `last_testcase`/`expected_output` into it. `capture.test.ts` D-01 test passes. |
| 2 | Capture is non-destructive and de-duplicated (identical failing case not written twice; existing cases never overwritten) | ✓ VERIFIED | D-04 (duplicate, no new file), D-05 (fill missing `.out`, never a new case), D-06 (disagreeing `.out` left byte-unchanged, surfaced as "mismatch") — all implemented in `capture.ts:78-102` and covered by dedicated passing tests (`capture.test.ts:43-108`), including a byte-for-byte-unchanged assertion for D-06. |
| 3 | The captured case is immediately gradeable offline — `leettui test` runs it and reports pass once fixed | ✓ VERIFIED | Round-trip test in `capture.test.ts:121-130` directly exercises `discoverCases`/`compareOutput` (the same primitives `leettui test`'s `testRunner.ts` uses) against a case written by `captureFailingCase`, and passes — a genuine behavioral proof, not just presence. |
| 4 | Capture never destabilizes the run/submit path: a parse/write failure degrades cleanly, result still renders | ✓ VERIFIED | Both `runSolution` and `submitSolution` wrap the capture call in `try { ... } catch { captureNotes = []; }` (`submission.ts:148-154`, `186-192`), on top of the writers' own never-throws contract. `submission.test.ts:264-281` simulates an ENOTDIR write failure via a bogus nested path and asserts `captureNotes` degrades to `[]`. |

**Score:** 4/4 ROADMAP success-criteria truths verified. All are directly proven by passing unit tests exercising real behavior (file writes, dedup logic, round-trip grading, throw-degradation) — not presence/grep alone.

### Plan-Level Must-Haves (frontmatter truths, both plans)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `captureFailingCase` writes a new `case-NN.txt` (+`.out`) when input matches no case (D-01) | ✓ VERIFIED | `capture.test.ts` D-01 test passes |
| 2 | `expectedOutput` undefined writes `.txt` only, no sibling `.out` (D-03) | ✓ VERIFIED | `capture.test.ts` "no match, expectedOutput undefined" test passes |
| 3 | Normalized-input duplicate not written twice (D-04) | ✓ VERIFIED | `capture.test.ts` D-04 test passes |
| 4 | Matching case with no `.out` gets it filled instead of duplicating (D-05) | ✓ VERIFIED | `capture.test.ts` D-05 test passes |
| 5 | Matching case with disagreeing `.out` left byte-unchanged, mismatch returned (D-06) | ✓ VERIFIED | `capture.test.ts` D-06 test passes, asserts byte-identical `.out` |
| 6 | `blessRunOutputs` fills missing `.out` matched by normalized content, never index (D-07) | ✓ VERIFIED | `capture.test.ts` "matching is by content not index" test passes (deliberately reordered case dir) |
| 7 | Captured case is gradeable offline via `discoverCases`/`compareOutput` round-trip | ✓ VERIFIED | `capture.test.ts` round-trip test passes |
| 8 | `normalize` exported from `testRunner.ts`, reused by `capture.ts`, no duplication | ✓ VERIFIED | `grep -q "export function normalize" src/core/testRunner.ts` succeeds; `capture.ts` imports it; no second copy of the trailing-whitespace/CRLF regex found in `src/` |
| 9 | `submitSolution` returns `{ response, captureNotes }`; `submit_wrong_answer` captures via `captureFailingCase` (D-01) | ✓ VERIFIED | `submission.ts:159-195`; `submission.test.ts` covers it |
| 10 | RE/TLE/MLE submit captures input-only (D-03) | ✓ VERIFIED | `captureFromSubmitResult` branch (`submission.ts:93-109`); `submission.test.ts` tests all three verdicts |
| 11 | `runSolution` returns `{ response, captureNotes }`; run_accepted/run_wrong_answer bless via `blessRunOutputs` (D-02) | ✓ VERIFIED | `submission.ts:126-157`; `submission.test.ts` `captureFromRunResult` tests |
| 12 | A capture/write failure never turns a successful run/submit into an error screen | ✓ VERIFIED | try/catch at both call sites (`submission.ts:148-154`, `186-192`); throwing-writer test passes |
| 13 | `ResultView` carries `notes?: string[]`, rendered dim below metrics/error in both inline panel and fullscreen | ✓ VERIFIED (code); ⚠ visual not exercised | `types.ts:36`, `ResultBody.tsx:150-159` — single shared component reaches inline/fullscreen/popup; no snapshot/visual test exists (SUMMARY 02.2-02 flags this itself as `human_judgment: true`) |
| 14 | CLI `presentResultView` prints capture notes (dim) below the result body | ✓ VERIFIED (code); ⚠ visual not exercised | `present.ts:195-198` |
| 15 | Capture notes never change verdict kind or CLI exit code | ✓ VERIFIED | `exitCodeForResultView` (`present.ts:244-254`) switches solely on `view.kind`, never touches `notes`; grep confirms no other reference |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/solutions/capture.ts` | `captureFailingCase`/`blessRunOutputs` writers | ✓ VERIFIED | Exists, substantive (131 lines, full D-01..D-07 logic), wired via barrel + `submission.ts` import |
| `src/core/solutions/capture.test.ts` | 14+ unit tests covering all branches | ✓ VERIFIED | 14 tests present and passing |
| `normalize` export in `src/core/testRunner.ts` | changed from module-private to exported | ✓ VERIFIED | `export function normalize(` confirmed via grep |
| TCASE-04/TCASE-05 rows in `.planning/REQUIREMENTS.md` | formalized with traceability | ✓ VERIFIED | Both present, marked `[x]` complete, traceability table rows `Complete`, coverage counts 23/23 |
| `notes?: string[]` on `ResultView` | (`src/views/browse/resultView/types.ts`) | ✓ VERIFIED | Line 36 |
| capture hooks + `{ response, captureNotes }` in `src/core/submission.ts` | | ✓ VERIFIED | `runSolution`/`submitSolution` both updated |
| `captureNotes` threaded through `handlers.ts` + `cli/index.ts` | | ✓ VERIFIED | Both call sites destructure and attach `notes` |
| `view.notes` render blocks in `ResultBody.tsx` + `present.ts` | | ✓ VERIFIED | Both present, correctly positioned (after error, before cases) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `capture.ts` | `../add-case`, `../testRunner` | `import { addCase } from "./add-case"; import { compareOutput, discoverCases, normalize } from "../testRunner";` | ✓ WIRED | Confirmed at `capture.ts:27-28` |
| `src/core/solutions/index.ts` (barrel) | `capture.ts` | re-export | ✓ WIRED | `submission.ts:9` imports `captureFailingCase, blessRunOutputs, getTestsDir` from `./solutions` |
| `submission.ts` return-shape change | 4 call sites (2 TUI + 1 CLI) | destructure `{ response, captureNotes }` | ✓ WIRED | `handlers.ts:323,353`, `cli/index.ts:290-306`; `tsc --noEmit` exits 0 confirms no missed site |
| `ResultView.notes` | `ResultBody.tsx` + `present.ts` | single view-model, two renderers | ✓ WIRED | Both files reference `view.notes` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `normalize` exported and reused (no second implementation) | `grep -rn "\.replace(/\\\\r\\\\n/g" src/` | Only one implementation, in `testRunner.ts` | ✓ PASS |
| Full test suite (single run) | `bun test` | 441 pass, 0 fail | ✓ PASS |
| `capture.test.ts` + `submission.test.ts` isolated | `bun test src/core/solutions/capture.test.ts src/core/submission.test.ts` | 33 pass, 0 fail | ✓ PASS |
| Typecheck | `bun run typecheck` | exit 0 | ✓ PASS |
| Lint (zero-tolerance) | `bun run lint` | "Checked 194 files... No fixes applied." | ✓ PASS |
| No capture config knob | `grep -ri "auto.capture\|captureEnabled" src/config src/core/submission.ts` | no matches | ✓ PASS |
| Debt markers in phase-modified files | `grep -n "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 9 phase-modified files | no matches | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention in this repo, and neither PLAN nor SUMMARY declares any probe scripts. **Step 7c: SKIPPED (no probes declared or found).**

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| TCASE-04 | 02.2-01, 02.2-02 | Submit WA/RE/TLE/MLE auto-capture into new case + sibling `.out`, deduped/non-destructive | ✓ SATISFIED | `capture.ts` D-01/D-03/D-04/D-05/D-06 + `submission.ts` `captureFromSubmitResult`; REQUIREMENTS.md marked `[x]` Complete |
| TCASE-05 | 02.2-01, 02.2-02 | Run responses bless missing `.out`s for seeded cases, content-matched not index-matched | ✓ SATISFIED | `capture.ts` `blessRunOutputs` D-02/D-07 + `submission.ts` `captureFromRunResult`; REQUIREMENTS.md marked `[x]` Complete |

No orphaned requirements: `grep -n "Phase 2.2" .planning/REQUIREMENTS.md` shows only TCASE-04/TCASE-05, both accounted for in both plans' `requirements` frontmatter.

### Anti-Patterns Found

None. Scanned all 9 files modified across both plans (`capture.ts`, `capture.test.ts`, `testRunner.ts`, `src/core/solutions/index.ts`, `.planning/REQUIREMENTS.md`, `submission.ts`, `submission.test.ts`, `types.ts`, `handlers.ts`, `cli/index.ts`, `ResultBody.tsx`, `cli/present.ts`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` and stub-shaped patterns (`return null`, hardcoded empty arrays/objects flowing to render) — none found. No debt markers.

### Human Verification Required

1. **TUI capture-note rendering** — Trigger a real WA submit (or a completed run) through `R`/`s` in `ProblemView` and confirm the dim note line appears correctly positioned in the inline Result panel, `ResultFullscreen` takeover, and browse `ResultPopup`, with no verdict-color change.
   - Expected: A dim line like "Captured case-05 (input + expected)" appears after error/metrics, before the cases list.
   - Why human: No snapshot/visual test exists for `ResultBody.tsx` in this codebase (SUMMARY 02.2-02 flags this itself). Code inspection confirms correct placement/logic but not actual terminal rendering.

2. **CLI capture-note rendering + live network round-trip** — Run `leettui run`/`leettui submit` against a real failing solution from a terminal and confirm the dim note prints correctly and the exit code is unaffected.
   - Expected: dim note after the error section; exit code matches pre-existing wrong/error semantics (1), unaffected by capture.
   - Why human: Same rendering concern on the CLI surface, plus this is the only path that exercises capture against a genuine LeetCode `CheckResponse` over the network — unit tests only cover the write logic against fabricated `ParsedResponse` objects and a temp `tests/` dir.

### Gaps Summary

No gaps found. All four ROADMAP success criteria and all 15 plan-level must-have truths are verified by passing, behavior-exercising unit tests (not presence-only checks) — including a genuine round-trip proof (criterion 3) and a throw-degradation proof (criterion 4). `bun test` (441/441), `bun run typecheck`, and `bun run lint` (zero-tolerance Biome) all pass cleanly. TCASE-04/TCASE-05 are correctly formalized and traced in REQUIREMENTS.md with no orphans. The two items routed to human verification are purely visual/rendering confirmations on already-code-verified, correctly-positioned render blocks — not functional gaps — so status is `human_needed` rather than `gaps_found`.

---

*Verified: 2026-07-11*
*Verifier: Claude (gsd-verifier)*
